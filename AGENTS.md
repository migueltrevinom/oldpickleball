# AGENTS.md

## Cursor Cloud specific instructions

This is a greenfield repository ("Pickleball super app") initialized with only a `README.md` and `LICENSE` (MIT). As of this writing, no application code, dependency manifests, build configuration, or services exist.

- **No lint, test, build, or run commands** are available until application code is added.
- **No dependencies** to install — there is no `package.json`, `requirements.txt`, `Cargo.toml`, or any other dependency file.
- **No services** to start — there is no backend, frontend, or database configuration.

When code is added, update this file with:
- The chosen tech stack and how to install dependencies.
- How to lint, test, build, and run the application.
- Any non-obvious caveats discovered during development.
